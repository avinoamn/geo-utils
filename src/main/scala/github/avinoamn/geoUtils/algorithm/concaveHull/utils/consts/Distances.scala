package github.avinoamn.geoUtils.algorithm.concaveHull.utils.consts

/** Distances constants. */
object Distances {
  /** Minimum buffer that is being used to calculate slope of vertical lines (Infinity/-Infinity). */
  val MIN_BUFFER: Double = Double.MinPositiveValue
}
